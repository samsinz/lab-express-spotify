<div>
  <h1>Albums</h1>
  <div>
    {{#each albums}}
      <picture>
        <img src="{{this.images.0.url}}" alt="" />
      </picture>
      <h2>{{this.name}}</h2>
      <p><a href="/tracks/{{this.id}}">View Tracks</a></p>
    {{/each}}
  </div>
</div>